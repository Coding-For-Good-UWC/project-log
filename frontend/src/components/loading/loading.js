import loading from "../../resources/loading_transparent.gif";

export default function Loading() {
    return (
        <div className="text-center">
            <img src={loading} />
        </div>
    );
}
